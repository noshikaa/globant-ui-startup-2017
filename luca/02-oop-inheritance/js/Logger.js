/*
* Class Logger
*/
class Logger {

    /*
    * Shows the info in the console.
    * input: info
    */
    log(info) {
        console.log("The " + info + " event is playing");
    }
}

module.exports = Logger;
